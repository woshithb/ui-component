export enum ButtonType {
  Primary = 'parmary',
  Ghost = 'ghost',
  Dashed = 'Dashed',
  Link = 'link',
  Text = 'text',
  Default = 'default'
}

export enum ButtonShape {
  Circle = 'circle',
  Round = 'round',
  Default = 'default'
}

export enum ButtonSize {
  Small = 'small',
  Middle = 'middle',
  Large = 'large'
}