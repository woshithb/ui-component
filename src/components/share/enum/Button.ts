export enum ButtonType {
  Primary = 'parmary',
  Dashed = 'dashed',
  Link = 'link',
  Text = 'text',
  Default = 'default',
}

export enum ButtonShape {
  Circle = 'circle',
  Round = 'round',
}

export enum ButtonSize {
  Small = 'small',
  Middle = 'middle',
  Large = 'large'
}