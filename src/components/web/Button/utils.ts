import { IButtonProps, IBaseButtonProps } from '@components/web';

export const propsTransfer = (props: IButtonProps): IBaseButtonProps => {
	const {
		block,
		danger,
		disabled,
		ghost,
		href,
		htmlType,
		icon,
		loading,
		shape,
		size,
		type,
		onClick,
  } = props;
  
};
